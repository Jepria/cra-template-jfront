import React from "react";
import { Tab, TabPanel } from "@jfront/ui-core";

const AppTabPanel = () => {
  return (
    <TabPanel>
      <Tab selected={true}>Main</Tab>
    </TabPanel>
  );
};

export default AppTabPanel;
